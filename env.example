# ========================================
# GESTOR PRO - CONFIGURAÇÃO DE AMBIENTE
# ========================================

# ===== CONFIGURAÇÃO DO BANCO DE DADOS =====

# Opção 1: URL completa (recomendado para produção)
DATABASE_URL=postgresql://usuario:senha@localhost:5432/gestor_pro?sslmode=require

# Opção 2: Variáveis individuais (recomendado para desenvolvimento)
PGDATABASE=gestor_pro_dev
PGUSER=postgres
PGPASSWORD=123456
PGHOST=localhost
PGPORT=5432

# ===== CONFIGURAÇÃO DE SEGURANÇA =====

# Chave secreta para JWT (GERE UMA CHAVE ÚNICA E SEGURA!)
JWT_SECRET=sua_chave_secreta_muito_segura_aqui_2024

# Tempo de expiração do token JWT
JWT_EXPIRES_IN=24h

# ===== CONFIGURAÇÃO DO SERVIDOR =====

# Porta do servidor
PORT=3000

# Ambiente (development, production, test)
NODE_ENV=development

# ===== USUÁRIO ADMIN PADRÃO =====

# Credenciais do administrador inicial
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=admin@gestorpro.com

# ===== CONFIGURAÇÕES AVANÇADAS =====

# Logging (true/false)
ENABLE_LOGGING=true

# CORS origins (separados por vírgula)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ===== CONFIGURAÇÕES DE EMAIL (OPCIONAL) =====

# SMTP para envio de emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu_email@gmail.com
SMTP_PASS=sua_senha_de_app

# ===== CONFIGURAÇÕES DE ARQUIVOS =====

# Diretório para uploads
UPLOAD_DIR=./uploads

# Tamanho máximo de upload (em bytes)
MAX_FILE_SIZE=5242880

# ===== CONFIGURAÇÕES DE CACHE =====

# Redis URL (opcional, para cache)
REDIS_URL=redis://localhost:6379

# ===== CONFIGURAÇÕES DE MONITORAMENTO =====

# Sentry DSN (opcional, para monitoramento de erros)
SENTRY_DSN=

# ===== INSTRUÇÕES =====

# 1. Copie este arquivo para .env
# 2. Configure as variáveis conforme seu ambiente
# 3. NUNCA commite o arquivo .env no Git
# 4. Para produção, use variáveis de ambiente do servidor

# ===== EXEMPLOS DE CONFIGURAÇÃO =====

# Desenvolvimento Local:
# DATABASE_URL=postgresql://postgres:123456@localhost:5432/gestor_pro_dev
# NODE_ENV=development
# PORT=3000

# Produção (Render/Railway):
# DATABASE_URL=postgresql://user:pass@host:port/db?sslmode=require
# NODE_ENV=production
# PORT=process.env.PORT (definido pelo servidor) 